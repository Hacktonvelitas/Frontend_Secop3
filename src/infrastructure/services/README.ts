// This directory contains direct API service definitions.
// Services should use the httpClient to make requests.
// These services should be called by the concrete Repositories in the infrastructure layer.
export {};
